# n8n webhook: "webhook" = produzione, "webhook-test" = ambiente di test
# In development, se non impostato, viene usato "webhook-test" di default.
# Per forzare produzione in dev: VITE_N8N_WEBHOOK_TYPE=webhook
VITE_N8N_WEBHOOK_TYPE=webhook
